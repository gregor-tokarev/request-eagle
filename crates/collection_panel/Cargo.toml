[package]
name = "collection_panel"
version = "0.1.0"
edition = "2024"

[dependencies]
collection = { path = "../collection" }
gpui-kit = { workspace = true, features = ["component"] }
suffix = "1.3.0"

[dev-dependencies]
gpui-kit = { workspace = true, features = ["test-support"] }
request_eagle_theme = { package = "theme", path = "../theme" }
