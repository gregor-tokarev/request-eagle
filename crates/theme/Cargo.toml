[package]
name = "theme"
version = "0.1.0"
edition = "2024"

[dependencies]
gpui-kit = { workspace = true, features = ["component"] }
preferences = { path = "../preferences" }
serde_json = "1"

[dev-dependencies]
gpui-kit = { workspace = true, features = ["test-support", "tree-sitter"] }
